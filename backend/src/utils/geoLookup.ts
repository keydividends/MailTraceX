// geoLookup.ts
// Placeholder for resolving IP -> geo information
export async function lookupIp(ip: string) {
  // integrate with geoip or third-party service
  return { country: 'unknown' };
}

export default lookupIp;
